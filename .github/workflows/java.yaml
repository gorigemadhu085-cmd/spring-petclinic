name: Java-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: my-spc
    runs-on: self-hosted

    steps:
      - name: Git Checkout
        uses: actions/checkout@v4

      - name: Build Java with Maven
        run: mvn package