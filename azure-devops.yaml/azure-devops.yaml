trigger:
- main

pool: "myagentjava"
  

stages:
  - stage: 
      jobs:
        - job: 
          steps: 
            - task: maven@3
              inputs:
                mavenPOMFile: 'pom.xml'
                goals: 'packages'
                publishJUnitResults: ture
                testResultsFiles: '**/surefire-reports/TEST-*.xml'
               